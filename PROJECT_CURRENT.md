# 视频生产工作台：当前状态

> 更新时间：2026-08-03（Asia/Shanghai）
>
> 本文件只描述当前仍受支持的流程。详细规则见
> `docs/JIANYING_DRAFT_WORKFLOW_REQUIREMENTS.md`。

## 1. 当前产品流程

```text
选择素材目录中的原视频
→ 人工确认产品和每条视频的标签
→ 系统把原视频移动到“素材目录/产品”文件夹
→ 原视频按“产品-标签-可选序号.扩展名”重命名
→ 在内容文库分析参考文案并生成/审核文案、字幕与旁白
→ 在背景音乐上传音乐或从短视频链接提取音频，并试听、打标签
→ 人工选择文案、字幕/旁白和背景音乐
→ 直接生成无视频轨道的剪映专业版半成品草稿
```

工作台不再导入或复制归类素材，不再提供人工粗剪、音乐卡点、系统内精修、成片渲染和热播跟踪。

## 2. 当前有效规则

### 素材归类

- “素材归类”页面分为“产品与标签管理”和“素材归类”两个标签页。
- “产品与标签管理”采用左侧产品清单、右侧标签分类与标签名称的并列布局，各清单独立滚动，避免大量产品把标签管理推到页面底部。
- 页面不再长期展示本地归类历史清单；归类成功后停留在“素材归类”，只在该标签页上方提示栏显示本次移动数量和移动后的文件名。
- 产品、标签分类和标签名称的删除操作统一使用页面中央确认弹窗，并明确提示数据库关系影响及磁盘文件边界。
- 素材归类中的产品名称与视频选择字段使用等宽按钮列、等高控件和对齐的状态说明行。
- 素材归类中的标签分类和标签名称使用可输入下拉框：手动输入时模糊筛选，点击箭头时直接展开列表并用所选项覆盖原文字。
- 产品名称、标签分类、标签名称均支持独立新增、分页查看、修改和直接删除；产品和标签通过各自的新增名称输入框直接联想查询，不另设重复查询框；三个输入框在归类页也支持输入查询与单独保存。
- 标签是跨产品通用主数据，以“标签分类＋标签名称”共同保证唯一；标签不能修改所属分类，删除分类会同时删除分类下标签。
- 一次选择的一组视频只归属于一个产品；视频默认不选，支持全选、取消全选、单选和批量选择。
- 点击标签会应用到全部选中视频；每条视频在同一标签分类下只能保留一个标签名称，再选同分类标签会替换原选择，点击已统一选中的标签会批量移除。
- “确认归类并移动”执行期间按钮显示移动中并锁定重复提交；操作编号保存在浏览器，刷新页面后继续查询服务端状态，不能因刷新丢失等待状态。
- 素材归类直接操作用户选择的原视频，不建立“导入后等待粗剪”批次。
- 每条视频必须至少选择一个标签；允许同时选择不同分类的标签组合。
- 确认前完成目录、格式、标签、重复路径、媒体可解码性和目标重名检查。
- 目标文件夹名称为产品名称；目标文件名由产品名称和所选标签组成，重名时追加数字序号。
- 全部校验通过后才移动文件；移动中失败时回退本次已移动文件。
- 归类记录保存原路径、移动后路径、产品、标签和媒体信息。
- 确认归类只负责本次移动与重命名，不回退或维护后续文件状态；已归类文件允许再次人工选择和重新归类。
- 删除或修改产品、分类、标签只影响数据库主数据与关系，不移动、不改名磁盘上的既有视频。

### 内容文库

- “内容文库”页面分为“文案”“音色库”“旁白与字幕”三个二级标签页，音色库排在第二位；短标题和长口播稿统一属于文案。
- 输入一条参考文案后，百炼自动分析语言风格、用词偏好、情感基调、诉求重点，并推断受众年龄、性别、兴趣、消费水平、心理状态和最匹配的专家角色。
- 分析与专家角色由模型自由生成且不人工修改；分析完成后自动生成 5 条仿写，保持输入内容类型和大致长度。
- 输入的参考原文自动进入文案库并视为已采纳；5 条候选分别采纳或不采纳，允许部分采纳，不采纳必须填写原因。
- 当前 5 条全部审核后才可继续迭代 5 条；后续沿用固定参考资料、分析、专家角色及此前审核反馈。
- 未输入参考文案时，只从全局已采纳文案形成的语料快照分析和推荐；空库时阻止生成并提示先输入参考文案。
- 分析与迭代记录最新在前、每页 10 条且可删除；候选与文案库条目物理分离，删除任一侧都不损坏另一侧。
- 分析、根据已采纳文案推荐及继续迭代执行期间保持生成中锁定；刷新页面后恢复原操作等待状态，完成或失败后才允许再次提交。
- 分析与迭代记录在所选条目正下方展开；同一个条目按钮负责“查看／收起”切换，不重复放置收起按钮，也可直接切换查看另一条。
- 文案库只显示已采纳内容，条目支持修改与中央确认删除，不在条目上堆叠分析来源。
- 文案输入、分析结果、迭代候选、历史摘要和文案库正文使用清晰的正文级字号与行距；文案删除成功后立即从当前列表移除，接口失败时保留确认框并显示错误。
- “文案”标签页包含音频转文案，支持抖音视频短链接、本地视频和本地音频；识别结果可以修改，人工点击后才保存到文案库，不创建旁白资源。
- 音频转文案使用百炼非实时 `qwen3-asr-flash` 的 OpenAI 兼容 `chat/completions` 协议，以 Base64 `input_audio` 传入音频；不再错误调用不存在的 `/audio/transcriptions`。调用前统一压缩为16kHz单声道MP3，并校验5分钟/10MB限制。模型列表和保存接口会排除协议不同的 `realtime`、`filetrans` 模型，运行时也会再次拒绝误配；百炼对 429/偶发 5xx 或网络错误最多自动尝试3次，并以同一业务调用编号逐次记录结果。
- 本轮已确认此前500失败实际误用了 `qwen3-asr-flash-realtime`，当前数据库配置已修正为 `qwen3-asr-flash`；并使用库存中文试听音频完成真实百炼识别，准确返回原试听文案。
- “旁白与字幕”标签页只保留字幕配音和资源库；配音文案可手动输入，也可从文案库选择。
- 字幕配音的“从文案库选择”使用模糊联想输入框，按关键词实时查询完整文案库、每次仅展示 10 条结果，选择后自动填入旁白正文；不受工作台首页预载 200 条数据限制，适合文案长期增长。
- 字幕配音只接受音色库序号，不再提供系统/附加音色栏，也不在模型配置中维护附加音色 ID。
- 音色试听首次点击时真实调用模型，成功后把音频文件及序号、voice、模型、路径、字节数写入数据库；后续试听直接读取库存音频，不重复调用模型。
- `qwen-audio-3.0-tts-plus` 试听与正式配音使用百炼工作区原生非实时 `SpeechSynthesizer` 接口，不再错误调用兼容模式 `/audio/speech`；系统从响应的临时 URL 下载 WAV。
- 音色库收录官方 597 条 Plus 基础音色，固定保留序号、名称、voice 参数、性别、年龄、特质、适用场景并支持试听；查询覆盖除试听外的全部目录字段，性别、年龄、适用场景提供独立下拉筛选，年龄按数值升序排列，每页 20 条。
- 音色库查询界面对后端筛选元数据缺失提供兼容兜底，前后端发布短暂不同步时不再导致页面白屏。
- 字幕配音必须输入 1–597 音色序号，自动解析对应 voice 参数后生成旁白；无效或空序号被前后端阻止。
- 模型配音不依赖音频转文案模型，使用已知正文和合成音频时长生成字幕时间轴；生成的旁白支持试听、确认和删除。
- “生成旁白配音”调用模型期间显示旋转图标、“正在生成旁白配音”按钮文字和独立任务说明，并锁定文案、音色序号与重复提交；完成或失败后恢复操作。所有后端媒体脚本和模型调用统一遵守“立即显示处理中状态、锁定重复提交、明确完成或失败”的交互规则，本轮同时补齐音频转文案、音乐上传和剪映草稿生成的状态说明。

### 背景音乐

- 背景音乐支持本地音频/视频上传，以及从短视频分享链接分离音频。
- 链接提取失败不阻塞其他音乐，用户可改为上传本地文件。
- 背景音乐可试听、改名，并可维护多个自定义标签；试听采用单播放器，再点当前音乐停止，切换音乐时先停止上一首，不能叠加播放。
- 短视频链接提取与本地上传分别维护音乐名称，互不串值；链接提取和上传均显示处理中状态并阻止重复提交。音乐试听显示加载状态与具体失败原因，音乐条目可通过居中确认框删除；已被剪映草稿引用的音乐仍禁止删除。
- 音乐条目标签编辑使用与删除确认框一致的居中遮罩弹窗，采用提取背景音乐按钮的绿色主题；支持逗号分隔、回车保存和清空全部标签。
- 背景音乐入库会检测有效音量，接近全静音的提取结果不再标记为可用音乐；本轮核查当前3个库存 WAV，发现2个约2.23秒的文件为全静音、1个约237秒的文件音量正常。
- 抖音匿名浏览器回退不再直接采用页面中的第一个媒体地址；优先解析真实 `video/source`，最多逐一下载并验证12个可信候选流，选择首个包含有效声音的媒体，避免把平台静音占位流入库。
- 抖音背景音乐解析优先识别页面独立 `audio`/`ies-music` 资源，并严格放行 `douyinstatic.com`、`byteimg.com` 的音乐路径；Windows Chrome 无头回退增加 WSL 兼容参数。已用最近失败的真实分享链接验证，成功提取约19秒、3.35MB的有效 WAV，临时验证文件随后已清除。
- 当前音乐实体不保存 BPM、节拍点、波形或卡点分析字段。

### 剪映草稿

- 用户不再选择产品和视频；视频由用户进入剪映专业版后自行添加、裁剪和排列。
- 文案、字幕/旁白和背景音乐均可不选，但一次生成至少选择一种物料。
- 草稿总时长取字幕最后结束时间、旁白音频时长、背景音乐时长的最大值；仅选择文案且无时间属性物料时，系统创建 5 秒最小草稿。
- 短于草稿总时长的旁白和背景音乐保持原始长度，不循环、不拉伸、不补静音。
- 文案写入独立文字轨道，从 0 秒持续到旁白真实音频或有效字幕结束；仅有文案或仅有背景音乐时，文案最多显示 5 秒。字幕写入另一条文字轨道，直接使用资源库已保存时间轴，不重新识别、不对齐到背景音乐；如果字幕时间轴异常长于旁白音频，则按旁白真实音频时长裁剪。
- 生成前自动检测本机剪映草稿目录，显示完整路径并由用户确认；未检测到目录时允许手动填写，确认有效后保存并优先复用。
- 草稿直接写入确认的剪映草稿目录，包含 `draft_content.json`、`draft_info.json`、`draft_meta_info.json`、剪映基础 sidecar 文件和使用说明；不生成 ZIP。为兼容当前剪映时间线，草稿保留空视频轨道但不写入任何视频素材或视频片段；旁白和背景音乐复制到草稿本地 `assets/audio` 后由音频轨道引用。
- 禁止覆盖同名草稿目录，存在同名目录时自动追加 `-2`、`-3` 等递增序号。
- 草稿名称自动追加 `YYYYMMDD-HHMMSS` 时间戳；剪映草稿记录可删除，删除只移除工作台记录，不删除磁盘上的草稿文件夹。
- 剪映草稿页面第一行显示目录确认，目录输入框和“重新检测／确认目录”按钮保持同一行，完整路径提示独占下一行且过长省略；第二行显示已选物料清单和生成按钮；第三行起按“文案内容库、旁白与字幕库、背景音乐库”三列分页选择，每库每页 20 条。三库均支持模糊查询，被省略的条目文字鼠标悬浮展示完整内容，旁白与字幕库、背景音乐库支持在剪映草稿页直接试听。剪映草稿记录也按最新在前分页显示，每页 20 条。
- 使用相同文案、旁白与字幕、背景音乐组合再次生成草稿时，前端弹窗提示该组合此前已生成次数；用户确认后仍可继续生成。该次数来自独立累计生成计数器，不随剪映草稿记录删除而减少。弹窗提供计数器复位按钮，复位只记录当前组合的计数基线，不删除草稿记录，也不阻止后续生成。后端在草稿快照中记录本次创建前的重复使用次数。

### 模型配置

- 当前新流程统一使用阿里云百炼兼容连接。
- 模型按两类业务展示：文案生成、语音模型。
- 文案生成负责文案分析、5 条迭代与字幕文案。
- 语音模型内部按功能分别配置“音频转文案”和“字幕配音”的模型 ID。
- 模型配置按“文案生成、语音识别、字幕配音”三张卡片从上到下纵向排列；每张卡分别配置百炼连接、独立读取模型列表、独立保存，只提交当前功能配置，不连带保存其他卡的未保存编辑；不再提供“百炼统一配置”和“复用文案生成连接”。
- 三个功能可以复用百炼接口地址和 API Key，也可以选择不同模型类别。
- 模型配置不再有启用复选框；配置完整即使用。读取模型列表后，模型类别使用可见的下拉列表；附加音色 ID 字段已移除。
- 连接测试和读取模型列表不计入真实业务调用日志；真实生成、识别和合成继续记录脱敏日志。

## 3. 已退役功能

- 归类后的素材导入/复制流程。
- 人工粗剪、粗剪代理、虚拟片段入库和片段标签编辑。
- 音乐节拍方案、卡点初拼和同质化检查。
- 生产计划自动选片、系统内时间线精修和成片渲染。
- 成片库、热播短链接识别、观察期与热播状态。
- 旧自动混剪、无人值守生产、模型素材筛选和模型审片。
- 框架库及框架布局功能。

退役数据表、字段和 ORM 声明已从当前结构物理删除；历史 Alembic 迁移文件继续保留，确保旧库
可以顺序升级到当前精简结构，但历史对象不会出现在新库或当前 ORM 中。

## 4. 工程状态

| 项目 | 当前值 |
| --- | --- |
| 后端 | FastAPI、SQLAlchemy、PostgreSQL |
| 前端 | React + Vite；入口 `frontend/src/HumanApp.tsx` |
| 最新迁移 | `g59x1q3r8s02` |
| 数据库 | 已升级到 `g59x1q3r8s02 (head)`；19 张当前业务表，剪映草稿表已移除旧产品、视频和 ZIP 包字段 |
| 当前数据实体 | 用户/会话/审计、模型配置与日志、产品、分类/标签、已归类素材、文案分析/批次/候选/文案库、音色试听、旁白、音乐、剪映草稿 |
| Python 测试 | `32 passed`；包含模型配置单卡独立保存、百炼 Qwen3-ASR Base64 音频协议、实时/文件转写模型排除与保存拒绝、服务端错误自动重试、597 条音色全字段查询、性别/年龄/场景组合筛选与年龄数值排序、背景音乐静音拒绝、试听音频入库复用、音频转文案不创建旁白、按序号配音、旁白删除、剪映目录持久化、草稿名称时间戳、异常字幕时长裁剪、重复草稿组合计数与复位、删除草稿记录不重置重复计数和无视频草稿生成 |
| React 生产构建 | 通过，Vite `1580 modules transformed`；CSS 26.93 KB，JS 267.40 KB；本轮最终资源 `index-CXSU31O1.js` |
| 空库迁移验证 | 本轮 ORM 空库测试通过；当前 PostgreSQL 已从 `f48w0r2t5u79` 升级到 `g59x1q3r8s02 (head)` |
| 当前 API | OpenAPI 包含音频转文案、音色目录分页、持久试听、按序号配音、旁白试听与删除、剪映草稿目录检测/确认、无视频草稿生成、重复组合计数/复位和草稿记录删除；旧人工录音转字幕、附加音色和草稿 ZIP 下载接口已移除 |
| 源码结构 | ORM 当前 19 个实体；597 条目录保留为只读 JSON，只有实际生成的试听音频按需入库 |
| API 服务 | `product-video-automation.service`，active |
| 后台维护 | 模型日志留存清理由 Web 服务内置定时任务执行；独立 Worker 和 Redis 已移除 |
| 健康检查 | 返回 Web、FFmpeg 和 FFprobe 当前状态，不再检测 Blender |
| 工作台 | 本机 `http://127.0.0.1:8000/workbench/`；服务监听 `0.0.0.0:8000` 供局域网访问 |
| 最近备份 | `backups/codexwork-handoff-20260803-1647.tar.gz`；含源码、文档、storage、静态构建和 `backups/current/product_video-20260803-164644.sql` 数据库 dump；SHA256 见同名 `.sha256` |

旧标题策略、一次生成一条标题及其历史数据已由迁移物理清除。当前接口统一使用 `/human/copies`：首次请求完成分析与 5 条生成，继续迭代必须先完成上一轮审核；候选和文案库分表保存，互相删除不连带。全局操作提示和生成中状态继续保留。

## 5. 常用命令

```bash
.venv/bin/pytest -q
npm --prefix frontend run build
.venv/bin/alembic current
systemctl --user restart product-video-automation
curl -sS http://127.0.0.1:8000/api/health
```

服务是用户级 systemd 服务，不使用 `sudo systemctl`。

## 6. 已约定的界面功能名称

完整清单及调用方式见 `docs/UI_FUNCTION_CATALOG.md`。用户提出“列出约定功能清单”时，按该文件返回当前清单。

- **模糊联想输入框**：大量候选项使用；输入文字后按相似度提供建议，不单独展开全部。素材归类的“产品名称”使用此功能。
- **可输入下拉框**：少量候选项使用；支持输入、模糊筛选、箭头展开全部和直接替换选择。
- **系统原生文件选择器**：调用 Windows 系统文件窗口；素材归类的“选择视频”属于支持多选的系统原生文件选择器。

## 7. 仍需真实业务验收

- 在真实素材盘选择多条视频，验证跨目录移动、中文产品/标签命名、重名序号和失败回退。
- 用当前安装的剪映专业版打开无视频草稿目录，验证文案、字幕、旁白和音乐轨道，并确认手动添加视频后的版本升级兼容性。
- 使用实际抖音链接或用户本地媒体完成“提取/上传→音频转文案→修改→保存文案库”的整条业务验收；ASR 单独真实调用已经通过。
- 使用实际百炼 TTS 模型验收音色库首次试听入库和按序号字幕配音。
- 使用真实短视频平台链接验证音频提取失败提示及本地上传回退。

## 8. 已确认的下一项开发

当前无已确认待开发事项。真实素材盘、真实百炼语音模型和当前剪映专业版的端到端验证仍按第 7 节执行。
