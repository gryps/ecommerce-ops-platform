# 新会话启动提示词

请在项目 `/mnt/e/codexwork` 中继续开发视频自动化工作台。

开始工作前：

1. 完整阅读 `PROJECT_CURRENT.md` 和 `NEXT_TASK.md`。
2. 查看 `docs/CONTEXT_INDEX.md`，只读取本次需求对应的模块文档。
3. 需要确认跨模块业务规则时，再读取 `PROJECT_DECISIONS.md` 的相关章节。
4. 不要默认通读 `PROJECT_HANDOFF_ARCHIVE.md`；只有追溯历史原因时才按关键词读取局部。
5. 以当前代码、最新数据库迁移和测试结果判断实现状态，不要把历史文档中的“尚未接入”
   或旧路线图当作当前待办。

协作规则：

- 除高风险、不可逆操作，或我明确说“先讨论”外，所有开发需求默认直接执行，无须等待我确认。
- 不要重复询问已经写入当前文档的业务决定；发现文档与代码不一致时，先查明原因并修正。
- 保留工作区中已有改动，不覆盖或回退无关内容。
- 完成开发后运行完整 Python 测试和 React 生产构建；必要时重启用户级 Web 服务并检查
  `/workbench/`。
- 完成后更新 `PROJECT_CURRENT.md` 中的当前验证结果；只把真实未完成事项写入 `NEXT_TASK.md`。
- 历史开发过程如需留档，追加到 `PROJECT_HANDOFF_ARCHIVE.md`，不要扩大默认上下文。

当前先不要自行扩展需求。读完必要文档后，直接处理我随后提出的开发任务。
