# Codex Workspace Instructions

本文件记录 `/mnt/e/codexwork` 下所有项目都应继承的长期约定。

## 操作边界

- 默认只在 `/mnt/e/codexwork` 及其子目录内读取、修改和运行项目文件。
- 不主动离开 WSL 工作区处理无关目录。
- 不还原用户已有改动，除非用户明确要求。
- 新会话或新项目开发前，先读取 `/mnt/e/codexwork/PROJECT_MEMORY.md`，再读取具体项目文档。

## 界面布局约定

- 布局前先估算栏目容量和操作密度，再决定页面结构。
- 产品清单、素材清单、任务清单、批量编辑表格、结果审核列表、平台字段模板等可能增长到几十到几百项的栏目，默认使用页面主工作区或全宽区域。
- 这类高容量栏目不得安排在窄侧栏、小卡片、压缩面板或装饰性容器里；除非用户明确要求，侧栏只放筛选、状态、快捷操作和摘要。
- 表格和清单需要优先保证横向信息完整、批量选择、批量编辑、筛选、排序、分页或虚拟滚动的扩展空间。
- 如果一个区域承载批量生产流程，它的布局优先级高于说明文字和装饰视觉。

## 验证约定

- 涉及按钮、菜单、编辑入口、文件选择器、批量操作等交互改动时，优先做真实交互测试或现有测试工具验证。
- 如果只能完成构建、接口或静态检查，必须在交付时明确说明没有做真实交互测试。

## 电商运营平台约定

- 电商视频生产、电商图片生产、模型配置属于同一个电商运营平台。
- 新增功能时沿用已有工作台的菜单层级、信息密度和生产型工具布局。
- 导航按钮保留既有配色方案，选中项使用亮色，未选中项使用暗色。
- 电商图片生产的最终业务口径以 `/mnt/e/codexwork/PROJECT_MEMORY.md` 中“电商图片生产最终业务口径”和“平台链接与自动上传口径”为准；旧图片文档中的固定 8 张参考图和网页上传口径已被用户后续修正。
